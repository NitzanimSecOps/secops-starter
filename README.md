# SecOps — starter

Shared scaffold for multi-part project exercises. Your repo was generated from this
template by `gh student accept`; the layout is already correct — **do not move or
rename anything**. Write your code under `solutions/suite_<S>/exercise_<N>/` and push;
every push is graded and the score syncs to Codo. The tests are not in this repo.
